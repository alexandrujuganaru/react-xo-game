import styled from "styled-components";

const GameMenu = () => {
  return (
    <Container>
      <p>Game menu page</p>
    </Container>
  );
};

const Container = styled.main`
  display: flex;
`;

export default GameMenu;
